const sampleContacts = require('./contacts');
const common = require('./common');
const config = require('./config');

const utils = {
  sampleContacts,
  common,
  config,
};

module.exports = utils;